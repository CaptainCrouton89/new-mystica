{
  "name": "project-doc-scripts",
  "version": "1.0.0",
  "type": "module",
  "description": "TypeScript scripts for managing YAML-based project documentation",
  "scripts": {
    "build": "tsc",
    "clean": "rm -f *.js **/*.js && rm -f *.d.ts **/*.d.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
